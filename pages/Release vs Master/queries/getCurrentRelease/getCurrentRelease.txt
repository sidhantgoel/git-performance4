SELECT run.id as id,metric,action,value from run_meta RIGHT OUTER JOIN run on run.meta = run_meta.id WHERE gh_run_id = {{TableUtils.getCurrentSelectedRelease()[0]}} and metric IN ({{TableUtils.getMetricsToShow()}}) and gh_run_attempt = {{TableUtils.getCurrentSelectedRelease()[1]}} ORDER BY id;